# ## General macOS files
.DS_Store
.AppleDouble
.LSOverride

# ## Build artifacts
# These are generated by Xcode during the build process.
build/
DerivedData/

# ## Swift Package Manager
# Caches and build products for Swift Packages.
.build/
.swiftpm/

# While some teams commit Package.resolved to lock dependency versions,
# for a library, it's often better to leave it untracked to avoid
# conflicts for the consumers of your SDK.
# Package.resolved

# ## Xcode Project files
# User-specific settings for projects and workspaces.
*.xcodeproj/project.xcworkspace
*.xcodeproj/xcuserdata
*.xcworkspace/xcuserdata

# ## Playgrounds
# User-specific playground data.
timeline.xctimeline
playground.xcworkspace

# ## CocoaPods
# In case you decide to add CocoaPods dependencies in the future.
Pods/
Podfile.lock

# ## Carthage
# In case you use Carthage for dependencies.
Carthage/

# ## Fastlane
# Temporary files and reports from Fastlane automation.
fastlane/report.xml
fastlane/test_output

# ## Secrets & Configuration
# A good practice is to never commit files with sensitive keys or environment variables.
*.env
secrets.plist
