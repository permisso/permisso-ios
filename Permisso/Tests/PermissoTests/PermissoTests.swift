import XCTest
@testable import Permisso

final class PermissoTests: XCTestCase {
    func testExample() throws {
        // Write your test here and use APIs like `XCTAssertEqual(...)` to check expected conditions.
    }
}
